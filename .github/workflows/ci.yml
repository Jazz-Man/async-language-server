name: CI

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  CARGO_TERM_COLOR: always

jobs:
  checks:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          persist-credentials: false
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Configure Rust Cache PIng
        uses: actions/cache@v6
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            ~/.cargo/git/checkouts/
            ~/.dylint_drivers/
            ~/.rustup/toolchains/
            target/
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          # restore-keys: |
          #   ${{ runner.os }}-cargo-

      - name: Install cargo-nextest
        uses: taiki-e/install-action@nextest
      - name: Run battery
        run: make battery
      # - name: Docs
      #   run: cargo doc --workspace --no-deps
      #   env:
      #     RUSTDOCFLAGS: "-D warnings"

  # test:
  #   runs-on: ubuntu-latest
  #   name: test (${{ matrix.features }})
  #   strategy:
  #     matrix:
  #       features: ["--all-features", "--no-default-features"]
  #   steps:
  #     - uses: actions/checkout@v7
  #       with:
  #         persist-credentials: false
  #     - uses: ./.github/actions/rust-setup
  #     - name: Test (${{ matrix.features }})
  #       run: cargo test --workspace ${{ matrix.features }} --verbose

  # dylint:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v7
  #       with:
  #         persist-credentials: false
  #     - uses: ./.github/actions/rust-setup
  #     - name: Install dylint
  #       run: cargo install cargo-dylint dylint-link --locked
  #     # Complements ./.github/actions/rust-setup: rust-cache already owns
  #     # ~/.cargo (bin included) and prunes ./target of non-dependency
  #     # artifacts, so only what it never covers is cached here.
  #     - name: Cache dylint artifacts
  #       uses: actions/cache@v6
  #       with:
  #         path: |
  #           ~/.cargo/bin/
  #           ~/.cargo/registry/index/
  #           ~/.cargo/registry/cache/
  #           ~/.cargo/git/db/
  #           ~/.cargo/git/checkouts/
  #           ~/.dylint_drivers/
  #           ~/.rustup/toolchains/
  #           target/
  #         key: dylint-${{ runner.os }}-${{ hashFiles('dylint.toml') }}
  #     - name: Lint (stock suites + perfectionist)
  #       run: cargo dylint --all -- --all-targets

  # release:
  #   needs: [checks, test, dylint]
  #   if: github.ref == 'refs/heads/main' && github.event_name == 'push'
  #   runs-on: ubuntu-latest
  #   permissions:
  #     contents: write
  #   steps:
  #     - uses: actions/checkout@v7
  #       with:
  #         fetch-depth: 0
  #         persist-credentials: false
  #     - name: Bump version and push tag
  #       uses: anothrNick/github-tag-action@v1
  #       id: bump
  #       env:
  #         GITHUB_TOKEN: ${{ github.token }}
  #         MAJOR_STRING_TOKEN: '#major'
  #         MINOR_STRING_TOKEN: '#minor'
  #         PATCH_STRING_TOKEN: '#patch'
  #         NONE_STRING_TOKEN: '#none'
  #         TAG_PREFIX: v
  #         VERBOSE: false
  #     - name: Build changelog
  #       id: changelog
  #       uses: loopwerk/tag-changelog@v1
  #       with:
  #         token: ${{ github.token }}
  #     - name: Release
  #       uses: softprops/action-gh-release@v2
  #       with:
  #         token: ${{ github.token }}
  #         generate_release_notes: true
  #         append_body: true
  #         make_latest: true
  #         tag_name: ${{ steps.bump.outputs.tag }}
  #         name: ${{ steps.bump.outputs.tag }}
  #         body: ${{ steps.changelog.outputs.changelog }}

  # pages:
  #   needs: [release]
  #   if: github.ref == 'refs/heads/main' && github.event_name == 'push'
  #   runs-on: ubuntu-latest
  #   environment:
  #     name: github-pages
  #     url: ${{ steps.deployment.outputs.page_url }}
  #   permissions:
  #     contents: read
  #     pages: write
  #     id-token: write

  #   steps:
  #     - uses: actions/checkout@v7
  #       with:
  #         persist-credentials: false
  #     - uses: ./.github/actions/rust-setup
  #     - name: Docs
  #       run: cargo +nightly doc --workspace --no-deps
  #       env:
  #         RUSTDOCFLAGS: "--enable-index-page -Zunstable-options"
  #     - name: Setup Pages
  #       uses: actions/configure-pages@v5
  #     - name: Upload artifact
  #       uses: actions/upload-pages-artifact@v3
  #       with:
  #         path: "./target/doc"

  #     - name: Deploy to GitHub Pages
  #       id: deployment
  #       uses: actions/deploy-pages@v5
