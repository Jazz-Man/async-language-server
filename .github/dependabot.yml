# To get started with Dependabot version updates, you'll need to specify which
# package ecosystems to update and where the package manifests are located.
# Please see the documentation for all configuration options:
# https://docs.github.com/code-security/dependabot/dependabot-version-updates/configuration-options-for-the-dependabot.yml-file

version: 2
updates:
  - package-ecosystem: "cargo" # See documentation for possible values
    directory: "/" # Location of package manifests
    target-branch: "develop"
    schedule:
      interval: "weekly"
      commit-message:
        prefix: "cargo: "
        include: "scope"
    ignore:
      # tree-sitter stays on 0.26 until tree-sitter-md (the downstream
      # Markdown grammar) ships a release supporting 0.27; two tree-sitter
      # majors cannot coexist in one dependency tree. See the hold comment
      # in Cargo.toml. Drop this entry when the grammar catches up.
      - dependency-name: "tree-sitter"
        versions: [">=0.27"]
