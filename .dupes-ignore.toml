[[ignore]]
fingerprint = "796b160440b3f2f3"
reason = "resolve hook twins (completion, code-action, inlay-hint): the lsp_request grammar binds one incoming and one outgoing hook fn per direction, each a one-line delegation to the file's item converter with only Direction flipped; the converters take distinct foreign item types, so unifying them is trait ceremony (successor of d9a381ff3f5dca75's impl-method form; family of 3ca1c2536ff13cb7)"

[[ignore]]
fingerprint = "1eb0db22997f837e"
reason = "spec-matrix test fixtures (TestServer/DisabledServer/ConfigurableServer document_diagnostics) in the #[cfg(test)] sibling module with_state/tests.rs: the bodies already delegate to the shared test_document_diagnostics helper — the residual twin is the trait-mandated declaration each Server impl must carry"

[[ignore]]
fingerprint = "1db0efb96bb722aa"
reason = "async-lsp LanguageServer notification forwards (did_open/did_close/did_save): tracing line, state-handler delegation, then the Server hook call; the trait mandates one method per notification, and the hook must run after the internal handler"

[[ignore]]
fingerprint = "e2c7af90d4455197"
reason = "WorkspaceDiagnosticsState atomic load-and-return accessors (supported, current_generation); the shared shape is the design, not drift"

[[ignore]]
fingerprint = "06f46feff88c5331"
reason = "DocumentMatcher builder pair with_url_globs/with_lang_strings: mirror-by-design extend of Into<String> items into the respective set"

[[ignore]]
fingerprint = "5e64d1f8cb621fc2"
reason = "examples' full_report helper appears in both minimal.rs and batch_diagnostics.rs; examples stay self-contained and didactically parallel by design"

[[ignore]]
fingerprint = "6c4db5f43053b240"
reason = "examples' server_capabilities builders (JsonServer, LongLineServer) are deliberately parallel so each example reads standalone"

[[ignore]]
fingerprint = "8fa261f1a1daff69"
reason = "WorkspaceDiagnosticConfig builder pair with_hidden_files/with_ignore_files: mirror-by-design delegation to the walker's corresponding options"

[[ignore]]
fingerprint = "99943b778a5e10cf"
reason = "node_at_position vs node_at_position_named differ only in descendant_for_point_range vs named_descendant_for_point_range; the tree-sitter API forces the twin"

[[ignore]]
fingerprint = "f9b5a480a3cae9d6"
reason = "test-support position/range builders in src/testing.rs and the tree_sitter_utils test module (both #[cfg(test)]): two-line constructors of a fixed position or range"

[[ignore]]
fingerprint = "257658785e4bc44d"
reason = "two-case split_at spec fns in lsp_tests.rs (split_at_divides_the_range_at_a_relative_position after Batch A2's merge, split_at_boundaries): the lsp flavor builds positions inline with no per-case bindings — unlike the tree-sitter flavor whose let text lines keep its twins apart — so any two two-case split_at fns normalize to one AST; avoidance analysis: dropping the merged multiline case loses a row-offset mutation pin, and tuple asserts or spurious range bindings are code contorted to please the tool; the real fix folds the boundary rows into the merged fn but deletes the ungrouped split_at_boundaries, which Batch A2's surgical scope forbids — that fold-in is the owner's dissolution path"

[[ignore]]
fingerprint = "390d3562637a1839"
reason = "outgoing vec converters (conversion.rs helpers for locations, text-edits, call/type-hierarchy items; incoming/outgoing calls' per-file convert_response hooks): each a one-line delegation to convert_optional_vec with only the item converter differing; the lsp_request outgoing hook must name one fn per response shape, so the per-call wrappers exist only to bind their per-call item converter (family of e0894e2bcf1c1930)"

[[ignore]]
fingerprint = "f83853ca365213c9"
reason = "capture-server bodies in with_state/tests.rs (link_resolve, hover): trait-mandated per-method Server impls sharing the capture-mutex fixture shape; each dispatch test needs its own echo/capture handler for its method"

[[ignore]]
fingerprint = "e0894e2bcf1c1930"
reason = "outgoing per-item range loops (document_links, code_lenses): the registry mandates one named outgoing helper per response shape, each a plain for-loop over items converting the item's range"

[[ignore]]
fingerprint = "7fc169e91fd317f7"
reason = "per-file `item` fixture builders in the four hierarchy test modules (incoming/outgoing calls, supertypes/subtypes): the lsp-types CallHierarchyItem/TypeHierarchyItem pair is structurally identical, so a crate::testing builder home would itself be a twin pair; the three-place pattern mandates per-method test modules with local fixtures"

[[ignore]]
fingerprint = "3ca1c2536ff13cb7"
reason = "convert_call_hierarchy_item/convert_type_hierarchy_item: the two lsp-types item structs share their field shapes but are distinct foreign types; unifying them would need a private access trait whose only value is dedup (ceremony per the crate's typing criterion), so the mirror pair stays"

[[ignore]]
fingerprint = "bf14980c7647a2e3"
reason = "resolve capture trio in with_state/tests.rs (code_lens, inlay_hint, workspace_symbol): trait-mandated per-method Server impls sharing the capture-and-echo shape over the common store helper — each dispatch test needs its own echo handler for its method (family of the f83853ca365213c9 capture-server bodies)"

[[ignore]]
fingerprint = "5accfbb29662c6ee"
reason = "workspace-symbol resolve standalone twins: the lsp_request standalone hooks bind one fn per direction, each delegating to the one document-free per-URL converter with only Direction flipped; the converter resolves each location against its own document, so no dispatch state needs an anchor (successor of ee7fe2f829bc4130's impl-method form; family of 796b160440b3f2f3)"

[[ignore]]
fingerprint = "958b58d9ab7eb467"
reason = "async-lsp LanguageServer notification forwards (did_change_watched_files, did_rename_files, did_delete_files): tracing line, state-handler delegation on the cloned params field, then the Server hook call; the trait mandates one method per notification and the hook must run after the internal handler (family of 1db0efb96bb722aa)"

[[ignore]]
fingerprint = "f7bd80e042774247"
reason = "async-lsp LanguageServer notification forwards without a tracing line (did_change_workspace_folders, did_change): state-handler delegation then the Server hook call; the trait mandates one method per notification, and the hook must run after the internal handler (family of 1db0efb96bb722aa)"

[[ignore]]
fingerprint = "0816e92c8d5f3c27"
reason = "HookRecordingServer hook impls in with_state/tests.rs (11 members): trait-mandated per-notification Server hook overrides sharing the one-line record-call shape, so the wiring test can pin that every hook fires (family of the f83853ca365213c9 capture-server bodies)"

[[ignore]]
fingerprint = "b89ab176db6368aa"
reason = "file-operations state handlers (handle_files_renamed, handle_files_deleted): a loop forwarding each entry's URI string to the shared remove_workspace_document_by_uri_string; FileRename/FileDelete are distinct foreign types with differently-named URI fields, so the mandated per-notification handlers mirror by design"

[[ignore]]
fingerprint = "3f792e39a2cd77aa"
reason = "hierarchy convert_params free fns (incoming/outgoing calls, supertypes/subtypes): the item converters take (state, document, &mut Item, Direction) while the lsp_request incoming hook takes the whole params, so a field-extraction wrapper is mandatory; unifying the four would need a field-access trait over four distinct foreign params types — dedup-only ceremony per the typing criterion (successor of e65ad068648fc2a0's impl-method form; family of 3ca1c2536ff13cb7's mirror converter pair)"

[[ignore]]
fingerprint = "8f2f2ab783a0dc96"
reason = "builder setters on unrelated types (ServerOptions::with_diagnostics_parallelism, DocumentMatcher::with_lang_grammar): each a one-line move-self field assign on its own type; avoidance analysis — a builder macro for two members is barred by macro-prefer-functions (the macros cycle dismantled exactly this class), a shared setter trait is premature abstraction whose only value is dedup, and reshaping either body to differ syntactically would be code contorted to please the tool (family of 06f46feff88c5331's builder mirror pair)"

[[ignore]]
fingerprint = "0bf28e4b604afeaa"
reason = "LongLineServer::document_diagnostics in minimal.rs and batch_diagnostics.rs: the duplication IS the example's lesson — the module doc says 'the same validation logic as minimal.rs, headless'; avoidance analysis — a #[path]-include of one example's server into the other breaks 'each example reads standalone' (5e64d1f8cb621fc2 precedent), moving the toy validator into the library puts policy into plumbing (product.md), a macro is barred by macro-prefer-functions (family of 5e64d1f8cb621fc2 and 6c4db5f43053b240's deliberately-parallel examples)"

[[ignore]]
fingerprint = "8cc63e9a2c94ab30"
reason = "oneshot test fixtures' server_options pins (TestServer width 1 for per-item determinism, GatedServer width 3 for the structural concurrency test): each pin is load-bearing for ITS test's determinism and the values differ on purpose; a shared fixture would couple the two widths the tests deliberately separate (family of fe902b81118cdf76's per-suite fixtures)"

[[ignore]]
fingerprint = "b784a964c3143666"
reason = "server_capabilities impls in examples/batch_diagnostics.rs (LongLineServer) and tests/termination.rs (ConfigurableServer): the example must read standalone (6c4db5f43053b240 precedent) while the test needs its own trait-mandated impl (f83853ca365213c9 capture-server precedent); the shared diagnostic_provider shape is the trait's, not drift"

[[ignore]]
fingerprint = "3ffa2d6b3cf079fc"
reason = "Configurable workspace-diagnostics server_options fixtures across tiers (wire RefreshServer in server/tests/workspace_diagnostics.rs, wrapper ConfigurableServer in with_state/tests.rs): the default_enabled values are load-bearing and opposite — the wrapper toggle test starts disabled so didChangeConfiguration is the enabler, the wire staleness test starts enabled so the same-value push pins the no-change path; a shared fixture couples suites the per-module layout keeps apart (fe902b81118cdf76/aee8589448a8b248 precedents) and cannot vary server_capabilities, which is an associated fn without self; dropping the no-op with_default_enabled(true) chain only moves the collision to termination.rs's identical no-chain fixture (b784a964c3143666 family), and Enabled/Disabled variants cannot drive these tests because every pin reads setting().is_some() (8cc63e9a2c94ab30 family of per-suite options pins)"

[[ignore]]
fingerprint = "d53443b60a97499b"
reason = "two-assert error pins in bytes_tests.rs (out_of_range_positions_return_position_out_of_range, sub_and_split_at_reject_invalid_positions after Batch A3's merge): the byte flavor's compact integer-arg shape makes any two two-assert unwrap_err fns normalize >=90% (257658785e4bc44d's lsp-flavor finding, here in bytes); each assert kills a distinct mutant (59fae27ce2e23bed's dissolved members), so dropping or reshaping either loses a kill; avoidance analysis — folding into the out-of-range fn would delete an ungrouped fn's identity and misname the StartAfterEnd pin (257658785e4bc44d's owner-dissolution precedent)"

[[ignore]]
fingerprint = "fc821d768b7bc965"
reason = "Batch A3's merged spec fns in bytes_tests.rs (split_off_returns_the_kept_side vs sub_resolves_relative_positions): two different methods' specs, each two cases over the same r(usize, usize)/TEXT vocabulary; the byte flavor has no shape-breaker (lsp builds inline nested positions, tree-sitter binds let text lines), so any two two-case fns normalize at the near threshold; the parallelism is the spec-matrix structure the file deliberately maintains (95df29debdfb5f1d/ab90a750f1ab7d48's dissolved reasons)"
